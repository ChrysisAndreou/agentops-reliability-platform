apiVersion: v1
kind: Service
metadata:
  name: agentops-api
  namespace: agentops
  labels:
    app.kubernetes.io/name: agentops-reliability-platform
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app: agentops-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
